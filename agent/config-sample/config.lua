--- test config for agent

config = {
    --- endpoint list
    --- format is: tcp_address:port or /unix/socket/path
    endpoints = {
         "0.0.0.0:12345"
        ,"0.0.0.0:12346"
        ,"127.0.0.1:12347"
    }
}
